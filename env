# Clave para acceso vía parámetro `?key=`
API_KEY=myapikey

# Puerto por defecto para desarrollo local
PORT=8080

# Ruta al archivo de credenciales de Google Cloud
GOOGLE_APPLICATION_CREDENTIALS=./credenciales.json